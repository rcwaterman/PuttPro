Flask==3.1.3
gunicorn==23.0.0
ultralytics
opencv-contrib-python==4.13.0.92
transformers>=4.44.0
# torch must be installed with the CUDA index URL for GPU support:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
